<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>DbmsProject3 · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <span><%= link "View all students", to: Routes.student_path(@conn, :index) %></span>
        <span><%= link "View all groups", to: Routes.group_path(@conn, :index) %></span>
        <span><%= link "View all educational levels", to: Routes.level_path(@conn, :index) %></span>
        <span><%= link "View all projects", to: Routes.project_path(@conn, :index) %></span>
      </section>
    </header>

    <section class="row">
      <%= form_for @conn, Routes.student_path(@conn, :index), [method: :get, class: "pull-right"], fn f -> %>
        <%= search_input f, :query_students %>
        <%= submit "Search by student parameters" %>
      <% end %>

      <%= form_for @conn, Routes.project_path(@conn, :index), [method: :get, class: "pull-right"], fn f -> %>
        <%= search_input f, :query_projects %>
        <%= submit "Search by project parameters" %>
      <% end %>

      <%= form_for @conn, Routes.project_path(@conn, :index), [method: :get, class: "pull-right"], fn f -> %>
        <%= search_input f, :query_projects_aggr %>
        <%= submit "See count of finished projects for year" %>
      <% end %>
    </section>

    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
